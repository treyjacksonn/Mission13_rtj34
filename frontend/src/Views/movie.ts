export type Movies = {
  //Creating React model from backend model
  movieId: number;

  category: string;
  title: string;
  year: number;
  director: string;
  edited: string;
  lentTo: string;
  notes: string;
};
